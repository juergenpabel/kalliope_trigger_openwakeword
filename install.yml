---
- name: openwakeword
  hosts: localhost
  gather_facts: no
  connection: local
  become: false

  tasks:
    - name: "Install pip dependencies"
      pip:
        name: openwakeword
        version: 1.4.0
        executable: pip3
